server:
  port: 8070

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    # service-url:   # Not required as we are having Standalone Eureka Server
    #   defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/